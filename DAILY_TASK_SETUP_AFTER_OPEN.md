# 📅 Windows定时任务设置指南 - 开盘后10分钟执行

## 🎯 目标

每个交易日美东时间9:40 AM(开盘后10分钟)自动执行日度策略:
- **夏令时**(3月-11月): 北京时间 21:40
- **冬令时**(11月-3月): 北京时间 22:40

---

## 📝 方法1: 使用任务计划程序(推荐)

### 步骤1: 打开任务计划程序

1. 按 `Win + R` 键
2. 输入 `taskschd.msc`
3. 按回车

### 步骤2: 创建基本任务

1. 点击右侧 **"创建基本任务"**
2. 名称: `Stock-Daily-Strategy-After-Market-Open`
3. 描述: `每个交易日开盘后10分钟执行日度策略`

### 步骤3: 设置触发器

#### 夏令时触发器(3-11月,北京时间21:40)

1. **触发器**: 每日
2. **开始**: 选择未来某个日期
3. **每隔**: 1 天
4. **时间**: `21:40:00`
5. **高级设置**:
   - ✅ 启用
   - 重复任务间隔: (留空)

#### 冬令时触发器(11-3月,北京时间22:40)

1. 添加第二个触发器
2. **触发器**: 每日
3. **时间**: `22:40:00`
4. 其他设置同上

**⚠️ 重要**: 需要在每年3月和11月手动切换启用/禁用对应的触发器!

### 步骤4: 设置操作

1. **操作**: 启动程序
2. **程序/脚本**: 
   ```
   K:\QT\run_daily_strategies_after_open.bat
   ```
3. **起始于(可选)**:
   ```
   K:\QT
   ```

### 步骤5: 设置条件

1. ⚠️ **取消勾选** "只有在计算机使用交流电源时才启动此任务"
2. ⚠️ **取消勾选** "只有在以下网络连接可用时才启动此任务"
3. ✅ **勾选** "如果过了计划开始时间,立即启动任务"

### 步骤6: 设置条件(高级)

1. **如果任务失败,重新启动**: 每隔 `1分钟`
2. **尝试重新启动**: `3` 次
3. **如果任务运行时间超过**: `1小时` (停止任务)
4. ✅ **不管用户是否登录都要运行**

### 步骤7: 完成

点击 **"完成"** 创建任务

---

## 📝 方法2: 使用PowerShell创建任务(高级)

### 夏令时任务(21:40)

```powershell
# 以管理员身份运行PowerShell

$action = New-ScheduledTaskAction -Execute "K:\QT\run_daily_strategies_after_open.bat" -WorkingDirectory "K:\QT"

$trigger = New-ScheduledTaskTrigger -Daily -At "21:40"

$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable

Register-ScheduledTask -TaskName "Stock-Daily-Strategy-Summer" -Action $action -Trigger $trigger -Settings $settings -Description "日度策略(夏令时21:40)" -RunLevel Highest
```

### 冬令时任务(22:40)

```powershell
$action = New-ScheduledTaskAction -Execute "K:\QT\run_daily_strategies_after_open.bat" -WorkingDirectory "K:\QT"

$trigger = New-ScheduledTaskTrigger -Daily -At "22:40"

$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable

Register-ScheduledTask -TaskName "Stock-Daily-Strategy-Winter" -Action $action -Trigger $trigger -Settings $settings -Description "日度策略(冬令时22:40)" -RunLevel Highest
```

---

## 🔧 高级配置: 只在工作日执行

如果想只在周一到周五执行(跳过周末):

### PowerShell方法:

```powershell
# 创建只在工作日触发的任务
$action = New-ScheduledTaskAction -Execute "K:\QT\run_daily_strategies_after_open.bat" -WorkingDirectory "K:\QT"

# 周一到周五触发
$trigger = New-ScheduledTaskTrigger -Weekly -DaysOfWeek Monday,Tuesday,Wednesday,Thursday,Friday -At "21:40"

$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable

Register-ScheduledTask -TaskName "Stock-Daily-Strategy-Weekdays" -Action $action -Trigger $trigger -Settings $settings -Description "日度策略(仅工作日)" -RunLevel Highest
```

---

## 📋 验证任务

### 检查任务是否创建成功:

```powershell
Get-ScheduledTask | Where-Object {$_.TaskName -like "*Stock-Daily*"}
```

### 手动测试任务:

```powershell
Start-ScheduledTask -TaskName "Stock-Daily-Strategy-Summer"
```

### 查看任务历史记录:

1. 打开任务计划程序
2. 找到任务
3. 点击 **"历史记录"** 选项卡

---

## ⏰ 执行时间对照表

| 时期 | 美东时间 | 北京时间 | 任务名称 | 状态 |
|------|----------|----------|----------|------|
| **夏令时** (3-11月) | 9:40 AM | 21:40 | Stock-Daily-Strategy-Summer | ✅启用 |
| **冬令时** (11-3月) | 9:40 AM | 22:40 | Stock-Daily-Strategy-Winter | ⏸️禁用 |

**🔄 切换提醒**:
- **每年3月第二个周日**: 禁用冬令时任务,启用夏令时任务
- **每年11月第一个周日**: 禁用夏令时任务,启用冬令时任务

---

## 🧪 测试建议

### 首次设置后的测试步骤:

1. **立即测试**:
   ```bash
   cd K:\QT
   run_daily_strategies_after_open.bat
   ```

2. **检查日志**:
   ```bash
   type logs\daily_strategy_execution.log
   ```

3. **检查邮件**:
   - 登录 qsswgl@gmail.com
   - 应该收到3封邮件(NVDA/TSLA/INTC)
   - 每封邮件应包含盘中实时价格

4. **验证实时价格**:
   - 邮件中应显示: "💹 盘中实时报价 (数据时间: 2025-XX-XX HH:MM:SS)"
   - 价格应为盘中实时数据,而非昨日收盘价

---

## ⚠️ 重要注意事项

### 1. 市场假期

任务会在周末和假期也执行,但会获取到最近一个交易日的数据。建议:
- 每年手动添加主要假期到任务计划的例外日期
- 或在脚本中添加假期检查逻辑

### 2. 网络连接

确保执行时网络连接正常:
- Finnhub API需要网络访问
- 邮件发送需要网络连接

### 3. 电源设置

- 如果是笔记本,确保21:40/22:40时电脑处于开机状态
- 建议设置电源计划,避免在执行时间休眠

### 4. 防病毒软件

某些防病毒软件可能阻止批处理文件执行:
- 添加 `K:\QT` 到白名单
- 或临时禁用实时保护

---

## 📊 执行流程

```
每日21:40/22:40 (北京时间)
    ↓
检查市场状态
    ↓
获取NVDA盘中实时价格 → 分析市场环境 → 运行策略 → 发送邮件
    ↓
获取TSLA盘中实时价格 → 分析市场环境 → 运行策略 → 发送邮件
    ↓
获取INTC盘中实时价格 → 分析市场环境 → 运行策略 → 发送邮件
    ↓
完成 (记录日志)
```

---

## 🔍 故障排查

### 问题1: 任务没有执行

**检查**:
1. 任务是否启用?
2. 触发器时间是否正确?
3. 查看任务历史记录中的错误信息

### 问题2: 执行但没有邮件

**检查**:
1. 查看 `logs\daily_strategy_execution.log`
2. 手动运行脚本测试
3. 检查QQ邮箱授权码是否有效

### 问题3: 获取不到实时价格

**检查**:
1. Finnhub API密钥是否有效
2. 网络是否正常
3. API调用次数是否超限(60次/分钟)

---

## 📞 下一步

设置完成后:
1. ✅ 测试任务手动执行
2. ✅ 等待第一次自动执行
3. ✅ 检查邮件中的实时价格
4. ✅ 根据邮件建议进行交易决策

---

**设置时间**: 2025-11-25  
**下次检查**: 每年3月和11月切换时令时任务
